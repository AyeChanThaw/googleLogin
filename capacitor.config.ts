import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lot.googlelogin',
  appName: 'googleLogin',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
